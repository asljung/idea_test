<% if signed_in? %>
  <div class="row">
    <aside class="col-md-3">
      <section>
        <%= render 'shared/user_info' %>
      </section>
       <section>
        <a href="/submit"><button type="button" class="btn btn-lg btn-primary">Submit an Idea</button></a>
 <!--        <%= render 'shared/idea_form' %> -->
      </section>
    </aside>
    <div class="col-md-9">
      <h3>Latest Idea Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <%= render 'shared/landing' %>
<% end %>