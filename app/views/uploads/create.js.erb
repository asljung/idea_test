<% if @upload.new_record? %>
  alert('Failed');
<% else %>
  if ($('#files') !== undefined) {
    jQuery(function() { $('a.swipebox').unbind("click");});
    $('#files').append("<%= j render(@upload) %>");
     jQuery(function() {$("a.swipebox").swipebox();});
  }
<% end %>
