<% if @upload.new_record? %>
  alert('Failed');
<% else %>
  if ($('#files') !== undefined) {
  	setTimeout(function(){
    	$('#files').append("<%= j render(@upload) %>");
    }, 3000);
  }
<% end %>
