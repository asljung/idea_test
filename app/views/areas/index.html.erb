<% provide(:title, 'Challenges') %>
<h1>Challenges</br><small><%= current_org.area_description %></small></h1>
<hr>
<div class="row">
  <% @areas.each_with_index do |area, index| %>
  	<% if index == 3 %>
  		</div><div class="row">
  		<% index==0 end %>
      <div class="col-md-4">
        <%= render partial: area %>
      </div>
  <% end %>
</div>