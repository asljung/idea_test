$('.ideas').append('<%= j render partial: "shared/idea", collection: @ideas %>');
$('a.load-more-ideas').attr('href', '<%= ideas_path(:page => @ideas.next_page, :search => params[:search]) %>');

<% unless @ideas.next_page %>
	$('a.load-more-ideas').remove();
<% end %>