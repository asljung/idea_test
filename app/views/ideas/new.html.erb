<% provide(:title, 'Submit new Idea') %>
<h1>Submit new Idea</h1>

<div class="col-md-6 col-md-offset-3">
  <%= form_for(@idea) do |f| %>
    <%= render 'fields', f: f %>
    <div class="col-md-6">
    <%= f.submit "Submit", class: "btn btn-lg btn-primary form-control" %>
    </div>
  <% end %>
</div>